doctype html
html(lang="en")
  head
    meta(name="apple-mobile-web-app-capable", content="yes")
    meta(name="mobile-web-app-capable", content="yes")
    meta(name="format-detection", content="telephone=no")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Torhi Calc
    script(type="text/javascript", src="/js/main.js")
    script(type="text/javascript", src="/js/fetch.js")
    link(rel="stylesheet", type="text/css", href="/css/main.css")
  body
    input(placeholder="Enter Calc", type="text", id="equate")
    #resspace
      #operation(style="display:none;") =
      #result(data-init="true") 0
    #keypad
      #keypadTop
        #cl(onclick="clearAll()") C
        #cl(onclick="clearError()") CE
      #keypadNums
        each val in ['7','8','9','4','5','6','1','2','3','0','.']
          #num(onclick="addDigit(this)")=val
      #keypadOps
        each op in ['=','+','-','÷','x']
          #op(onclick="produceCalc(this)")=op